[package]
name = "pmos-abi"
description = "Versioned syscall ABI — the only kernel dependency userland is allowed"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }
